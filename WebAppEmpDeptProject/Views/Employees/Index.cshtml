@model IEnumerable<EmpDeptProject.Models.Employee>
@{
    ViewData["Title"] = "Employees";
}

<partial name="_AlertPartial" />

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Employee List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Employees" asp-action="Create" class="btn btn-outline-primary"><i class="bi bi-plus-circle-dotted"></i> Add Employee</a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Employee Id
            </th>
            <th>
                Employee Name
            </th>
            <th>
                Employee Salary
            </th>
            <th>
                Department Name
            </th>
            <th>
                Edit | Delete
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var c in Model)
        {
            <tr>
                <td>@c.EmpId</td>
                <td>@c.EmpName</td>
                <td>@c.EmpSal</td>
                <td>@c.Departments.DeptName</td>
                <td>
                    <div class="btn-group m-75" role="group">
                        <a asp-controller="Employees" asp-action="Edit" asp-route-empId="@c.EmpId" class="btn btn-outline-primary"><i class="bi bi-pencil"></i></a>
                        <a asp-controller="Employees" asp-action="Delete" asp-route-id="@c.EmpId" class="btn btn-outline-primary"><i class="bi bi-trash3"></i></a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>


